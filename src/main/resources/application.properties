# Swagger / OpenAPI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
spring.application.name=server

# MongoDB Configuration
spring.mongodb.uri=mongodb+srv://lakmalimlmlml_db_user:Dinesh532@cluster0.buhy1pe.mongodb.net/laundrify
spring.mongodb.database=laundrify

# JWT Configuration
jwt.secret=laundrify-secret-key-for-jwt-ax`uthentication-2024
jwt.expiration=86400000
jwt.refresh.expiration=604800000

# Server Configuration
server.port=8080

# File Upload Configuration
file.upload.dir=uploads/drivers
file.upload.laundry.dir=uploads/laundry
file.upload.receipt.dir=uploads/receipts
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB
server.tomcat.max-part-count=20

# Email Configuration (SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.from=noreply@laundrify.com
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Application Frontend URL
app.frontend.url=http://localhost:3000
